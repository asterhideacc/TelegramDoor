# 真实账号验收

自动化测试模拟了 Telegram 与 Turnstile 返回值，不能替代真实平台的集成验收。部署前需要真实 Bot Token、管理员 ID、Cloudflare 账户；本文不假设已经完成云端验收。

## 部署

- [ ] 按[Fork 部署教程](deployment-manual.md)创建真正的 Fork，仓库显示 `forked from maodeyu180/TelegramDoor`。
- [ ] README 的 Cloudflare 入口打开控制台，在已有 Worker 的 Settings → Builds 选择自己的 Fork，不会再次复制模板。
- [ ] 创建专用 D1，在 Worker 控制台选择并保存为 `DB`；无需编辑 Fork 或复制 ID。
- [ ] 仓库包含 `src/`、`package.json`、`package-lock.json` 和 `migrations/`；只有 README 和 Wrangler 配置时按教程处理。
- [ ] D1 数据库实际存在于 Worker 所在账户，Worker 的 `DB` 指向该数据库；只有生成 ID 不算创建成功。
- [ ] 默认 Worker 名称使用 `telegramdoor`；自定义名称用部署命令 `npm run deploy -- --name 实际名称`，不需要修改 Fork。构建命令留空。
- [ ] 三个密钥保存到 Worker 运行时「变量和机密」，名称无多余空格，生产部署已生效。
- [ ] 确认 D1 已创建、绑定名是 `DB`；无需额外 D1 API Token 或手工运行 SQL，首次访问 `/health` 返回 `ok: true`。
- [ ] 后台能登录；刷新保持登录；退出后原 Cookie 无法访问 API。
- [ ] 再次部署后，已有设置、封禁、消息对应关系、DB 绑定和控制台变量/密钥仍保留。
- [ ] 所有管理页面和手机布局正常；HTTPS 静态资源与 API 不报 CSP 错误。
- [ ] 管理员向机器人 `/start` 后，在后台连接并检查 Webhook，URL 指向自己的域名。

- [ ] Sync fork 后触发最新提交的构建，原 Worker 名称、数据库绑定、设置和数据保持正确；按[更新说明](updating.md)处理旧版固定 ID 配置。
- [ ] 从独立副本迁到 Fork 时复用原 Worker 和 D1，运行时密钥仍生效。

## 可选模板部署

- [ ] [模板按钮](deployment.md#模板部署可选)询问三个密钥，创建完整独立副本并自动创建、绑定 D1。
- [ ] 首次 `/health` 正常；手工补建数据库的成功记录不能算作模板自动创建成功。

## 双向私信

- [ ] 用第二个账号发送未验证消息，管理员不收到，后台拦截记录出现。
- [ ] 完成验证，再发送普通文本、带格式文本、图片、视频、语音、文件、贴纸、相册。
- [ ] 确认相册当前逐条显示，其他类型符合 Bot API 支持情况。
- [ ] 管理员引用回复；访客再引用管理员的回复，验证双方消息关系正确。
- [ ] 两个访客交替留言和回复，确保不串人。
- [ ] 修改文本、媒体说明和支持编辑的媒体，检查对方更新。
- [ ] 编辑成被过滤的链接后，对方保持原消息，后台记录拦截。
- [ ] 使用后台回复，确认管理员镜像、访客消息及后续引用都正常。

## 回应

- [ ] 管理员点 👍 按钮，访客原消息显示机器人回应。
- [ ] 访客对管理员回复点击 ❤，管理员原消息显示回应。
- [ ] `/react 🔥` 和 `/react clear` 正常；撤销按钮正常。
- [ ] 不支持回应的消息显示操作失败，而不是显示成功。
- [ ] 私聊长按原生点赞不会被当作可同步能力宣传。

## 防骚扰

- [ ] `/ban`、`/ban 1h 原因`、显式 ID 封禁、后台封禁均有效。
- [ ] 被封禁用户继续发消息与 `/start` 时，不通知管理员，也不向用户持续回执；后台仍有记录。
- [ ] 旧的表情和验证按钮不能绕过封禁。
- [ ] `/unban` 后要求重新验证；临时封禁到期后可重新验证。
- [ ] 白名单可以跳过验证/过滤，但不能绕过限频和暂停。
- [ ] 验证失败 3 次进入冷却，冷却结束可重试；5 分钟过期挑战不能使用。
- [ ] Turnstile 在 Telegram 内置浏览器和常用浏览器中均可完成；错误域名、重复提交不能放行。
- [ ] 关闭文本记录后，新日志不再包含正文；保留期清理不删除黑名单。

## 运行

- [ ] 检查 Workers 请求量、CPU、D1 行读写量，确认符合自己的免费额度。
- [ ] 检查 Telegram 429 行为、用户屏蔽机器人行为以及 Worker 重试。
- [ ] 修改 Bot Token 后重新连接，并重新保存 Turnstile Secret。
- [ ] 导出/备份 D1，验证项目升级中的迁移过程。
